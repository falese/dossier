<table>
  <tr>
    <th>User Name</th>
    <th>Email</th>
  </tr>

  <tr>
    <td><%= @user.username %></td>
    <td><%= @user.email %></td>
  </tr>


</table>
<%= link_to 'Edit', edit_user_path(@user) %>
<%= link_to 'Add a file', new_user_attachment_path(@user, @attachment) %>
